import { DefaultChangeService } from './default-change-service';

describe('DefaultChangeService', () => {
  it('should create an instance', () => {
    expect(new DefaultChangeService()).toBeTruthy();
  });
});
